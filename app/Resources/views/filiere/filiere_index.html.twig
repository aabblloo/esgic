{% extends 'base.html.twig' %}
{% set titre='Liste des filières' %}

{% block title %}
    {{ titre }}
{% endblock %}

{% block titre %}
    {{ titre }}
{% endblock %}

{% block body %}
    <div class="breadcrumb">
        <a class="btn btn-link" href="{{ path('filiere_new') }}">
            <i class="fa  fa-chalkboard-teacher"></i>
            Enregistrer une filière
        </a>
    </div>

    <table class="table table-bordered- table-striped table-hover" id="myTable">
        <thead>
        <tr>
            <th>Code</th>
            <th>Nom</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for filiere in filieres %}
            <tr>
                <td>{{ filiere.code }}</td>
                <td>{{ filiere.nom }}</td>
                <td class="text-right">
                    <a href="{{ path('filiere_edit', {'id': filiere.id}) }}" class="btn btn-link py-0">
                        <i class="fa  fa-edit"></i>
                        Modifier
                    </a>
                    <a href="{{ path('filiere_delete', {'id': filiere.id}) }}" class="btn btn-link py-0 text-danger">
                        <i class="fa  fa-trash-alt "></i>
                        Supprimer
                    </a>
                </td>
            </tr>
        {% else %}
            <caption>Pas d'enregistrement</caption>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('DataTables/datatables.min.css') }}">
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('DataTables/datatables.min.js') }}"></script>

    <script>
        $(function () {
            $("#admin").addClass("active");
            $("#filiere_index").addClass("active");

            $('#myTable').DataTable({
                order: [],
            });

        });
    </script>
{% endblock %}
