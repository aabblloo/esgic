{{ form_start(form) }}
<div class="row">
	<div class="col-4">
		{{ form_widget(form) }}
	</div>
</div>

<div class="form-group mt-2">
	<button class="btn btn-primary">
		<i class="fas fa-save"></i>
		Enregistrer
	</button>
</div>
{{ form_end(form) }}

<p class="mt-3">
	<a href="{{ path('parametres_index') }}">Retour à la Liste</a>
</p>
